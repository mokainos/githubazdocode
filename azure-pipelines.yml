variables:
  configuration: debug
  platform: x64
stages:
  - stage: 
    displayName: stage1
    jobs:

      - job: 
        variables:
          key1: values1
        steps:
          - script: echo "s1j1s1 $KEY1  $(key1)"
